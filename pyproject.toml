[build-system]
build-backend = "hatchling.build"
requires = [ "hatchling" ]

[project]
name = "mina"
version = "0.1.0"
description = "Multicellular INtegration Analysis"
readme = "README.md"
license = { file = "LICENSE" }
maintainers = [
  { name = "Klaus Kruger", email = "klaus.kruger@stud.uni-heidelberg.de" },
  { name = "Ricardo Omar Ramirez Flores", email = "flores@ebi.ac.uk" },
]
authors = [
  { name = "Klaus Kruger" },
  { name = "Ricardo Omar Ramirez Flores", email = "flores@ebi.ac.uk" },
]
requires-python = ">=3.12,<3.14"
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
  "mofaflex @ git+https://github.com/bioFAM/mofaflex.git@main",
  "decoupler",
  "scanpy"
]

[project.optional-dependencies]
liana = [
  "liana", # required for ligand-receptor networks
]
spatial = [
  "squidpy", # required for spatial summaries
  "plotnine>=0.10.0", # required for MuVIcell-derived plotting helpers
]
patpy = [
  "patpy @ git+https://github.com/lueckenlab/patpy.git@main", # required for patient representation
]
extra = [
  "liana", # compatibility bundle for all optional runtime integrations
  "squidpy",
  "patpy @ git+https://github.com/lueckenlab/patpy.git@main",
]
dev = [
  "ruff",
  "pre-commit",
  "twine>=4.0.2",
]
doc = [
  "ipykernel",
  "ipython",
  "mkdocs>=1.6,<2",
  "mkdocs-jupyter>=0.26,<0.27",
  "mkdocs-material>=9.7.5,<10",
  "mkdocstrings[python]>=1,<2",
  "pymdown-extensions",
  "pandas",
]
test = [
  "coverage>=7.10",
  "pytest",
  'pytest-cov',
]
[project.urls]
# https://docs.pypi.org/project_metadata/#project-urls
Documentation = "https://saezlab.github.io/mina/"
Homepage = "https://github.com/saezlab/MINA"
Source = "https://github.com/saezlab/MINA"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = [ "src/mina" ]

[tool.hatch.envs.default]
installer = "uv"
features = [ "dev" ]

[tool.hatch.envs.docs]
dependencies = [
  "mkdocs>=1.6,<2"
]
features = [ "doc" ]
scripts.build = "mkdocs build --clean --strict {args}"
scripts.open = "python -m webbrowser -t site/index.html"
scripts.clean = "git clean -fdX -- {args:site}"

# Test the lowest and highest supported Python versions with normal deps
[[tool.hatch.envs.hatch-test.matrix]]
deps = [ "stable" ]
python = [ "3.13" ]

# Test the newest supported Python version also with pre-release deps
[[tool.hatch.envs.hatch-test.matrix]]
deps = [ "pre" ]
python = [ "3.13" ]

[tool.hatch.envs.hatch-test]
features = [ "dev", "test" ]

[tool.hatch.envs.hatch-test.overrides]
# If the matrix variable `deps` is set to "pre",
# set the environment variable `UV_PRERELEASE` to "allow".
matrix.deps.env-vars = [
  { key = "UV_PRERELEASE", value = "allow", if = [ "pre" ] },
]

[tool.ruff]
line-length = 120
src = [ "src" ]
extend-include = [ "*.ipynb" ]

format.docstring-code-format = true

lint.select = [
  "B",      # flake8-bugbear
  "BLE",    # flake8-blind-except
  "C4",     # flake8-comprehensions
  "D",      # pydocstyle
  "E",      # Error detected by Pycodestyle
  "F",      # Errors detected by Pyflakes
  "I",      # isort
  "RUF100", # Report unused noqa directives
  "TID",    # flake8-tidy-imports
  "UP",     # pyupgrade
  "W",      # Warning detected by Pycodestyle
]
lint.ignore = [
  "B008", # Errors from function calls in argument defaults. These are fine when the result is immutable.
  "D100", # Missing docstring in public module
  "D104", # Missing docstring in public package
  "D105", # __magic__ methods are often self-explanatory, allow missing docstrings
  "D107", # Missing docstring in __init__
  # Disable one in each pair of mutually incompatible rules
  "D203", # We don’t want a blank line before a class docstring
  "D213", # <> We want docstrings to start immediately after the opening triple quote
  "D400", # first line should end with a period [Bug: doesn’t work with single-line docstrings]
  "D401", # First line should be in imperative mood; try rephrasing
  "E501", # line too long -> we accept long comment lines; formatter gets rid of long code lines
  "E731", # Do not assign a lambda expression, use a def -> lambda expression assignments are convenient
  "E741", # allow I, O, l as variable names -> I is the identity matrix
]
lint.per-file-ignores."*/__init__.py" = [ "F401" ]
lint.per-file-ignores."docs/*" = [ "I" ]
lint.per-file-ignores."tests/*" = [ "D" ]
lint.pydocstyle.convention = "numpy"

[tool.pytest.ini_options]
testpaths = [ "tests" ]
xfail_strict = true
addopts = [
  "--import-mode=importlib", # allow using test files with same name
]

[tool.coverage.run]
source = [ "mina" ]
patch = [ "subprocess" ]
omit = [
  "**/test_*.py",
]

[tool.cruft]
skip = [
  "tests",
  "src/**/__init__.py",
  "src/**/basic.py",
  "docs/api.md",
  "docs/changelog.md",
  "docs/references.bib",
  "docs/references.md",
  "docs/notebooks/example.ipynb",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
